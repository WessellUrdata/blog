---
interface Props {
  href: string;
  alt: string;
  src: string;
}

const { href, alt, src } = Astro.props;
---

<a href={href} target="_blank" rel="noreferrer">
  <img src={src} alt={alt} style="width: 88px; height: 31px;" />
</a>
